1. Copy the prior JSON as a new file (e.g., _v1.1.json).
2. Update version, dates, and status to Draft (until approved).
3. Adjust sections/content; record changes in a Revision History table if used.
4. Regenerate; move previous version to \Archive\ per retention policy.