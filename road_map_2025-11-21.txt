
You now have:

Method 1 (JSON-first generated by ChatGPT or other sources) fully working

Method 2 (Word→Importer→JSON→Generator) fully working

Tables, lists, headings, numbering all stable

Mandatory ISMS frame sections always present

Document Control Register automation working

Master Reference Register automation working

This is already more automation than many ISO 27001–certified companies ever achieve.



Dropbox integration -- done

Method 1 -- template per document type (e.g. policy, procedure or record) to attach to ChatGPT prompt when asking to generate an ISMS document.


🔧 Optional Next Enhancements
Make the registers databases, not CSV files!

Auto-generate change summaries for the DCR (diff-style)

Auto-detect “references” inside section content (Phase 2 of MRR)

Introduce validation rules for specific doc types (Policy / Procedure conventions)

Add HTML / PDF output (if needed later)

Generate a “Register Dashboard” (CSV → Markdown summary)